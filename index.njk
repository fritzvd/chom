---
title: Cheeses of Mexico
url: https://cheesesofmexico.com
ogimage_400: https://https://cheesesofmexico.com/static/img/logo_400.png"
---


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <meta name="description" content="Website for Cheeses of Mexico">

    <meta property="og:title" content="{{ title }}" />
    <meta property="og:type" content="music:song" />
    <meta property="og:description" content="Song page for {{ title }}">
    <meta property="og:url" content="{{ url }}">

    <meta property="og:image" content="{{ ogimage_400 }}">
    <meta property="og:image:type" content="image/png">
    <meta property="og:image:width" content="400">
    <meta property="og:image:height" content="400">

    <link rel="shortcut icon" href="/static/img/favicon.png" type="image/png">
    <link rel="stylesheet" href="/static/css/style.css">

    <title>Cheeses of Mexico</title>
    <style>
        body {
            font-family: "Helvetica Neue Condensed Bold", "Arial Black", Arial sans-serif;
            display: grid;
            /* flex-direction: column; */
            background: url(static/img/bee2.png);
            background-repeat: repeat-y;
            background-position-x: center;
            justify-items: center;
        }
        header {

            background-color: unset;
        }
        h1 {
            color: #104dfc;
            font-style: normal;
            text-transform: uppercase;
            font-weight: 900;
            font-size: 4em;
        }

        main {
            background-color: rgba(255, 255, 255, 0.5);
            justify-self: center;
            width: 80vw;
        }

        .intro {
            text-align: center;
        }

        .intro a {
            color: #104dfc;
            background-color: white;
            transition: all linear 200ms;
            border-radius: 5px;
            padding: 0 1em;
        }

        .intro a:hover {
            color: #012da8;
            background-color: rgb(176, 176, 176);
            transition: all linear 200ms;
            border-radius: 5px;
        }

        .album img {
            width: 400px;
        }

        .album a {
            display: flex;
            flex-direction: column;
            margin: 0 1em;
            padding: 1em;
            background-color: white;
            text-decoration: none;
            color: #03907e;
        }
        
        .album a:hover {
            padding: 1em;
            color: #025d51;
            text-decoration: underline;
            background-color: white;
        }

        .album p {
            text-align: center;
        }
        
        .albums {
            display:flex;
            flex-wrap: wrap;
        }

        article.album {
            filter: blur(2px);
            transition: all linear 300ms;
        }

        article.album:hover {
            filter: blur(0);
            transition: all linear 300ms;
        }

        iframe {
            height: 340px;
            width: 100%;
        }

        @media (max-width: 850px) {
            body {
                overflow-x: hidden;
            }

            iframe {
                width: 100vw;
            }
            main {
                width: 100vw;
            }
            .album  {
                width: 300px;
            }

            .album img {
                width: 100%;
            }
        }

        @media (max-width: 650px) {
            iframe {
                width: 100vw;
            }
            main {
                width: 100vw;
            }
            .album  {
                width: 100vw;
            }

            .album img {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>{{ title }}</h1>
    </header>
    <main>
        <section class="intro">
            <p>We are Cheeses of Mexico. A band from the Gelderland province in the Netherlands.</p>
            <p>Contact us: <a href="mailto:cheesesofmexico+website@gmail.com">cheesesofmexico at gmail dot com</a>.</p>
            <iframe src="https://www.youtube-nocookie.com/embed/Oy_hPonFO9g" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
        </section>
        <section>
            <h2>Here's a list of albums we made. You can listen to them</h2>
            <div class="albums">
                {% for a in album %}
                    <article class="album">
                        <a href="/album/{{a.name}}">
                            <img src="/static/img/covers/{{a.name}}.png" alt="Album cover for {{a.title}}">
                            <p>{{ a.title }}</p>
                        </a>
                    </article>
                {% endfor %}
            </div>
        </section>
        <section class="boringstranger">
            <h2>Here's a list of releases by our lead singer</h2>
            <div class="albums">
                {% for a in boringstranger %}
                <article class="album">
                    <a href="/boringstranger/{{a.name}}">
                        <img src="/static/img/covers/{{a.name}}.png" alt="Album cover for {{a.title}}">
                        {{ a.title }}
                    </a>
                </article>
                {% endfor %}
            </div>
        </section>
        <section class="dancingonglass">
            <h2>Here's a list of releases by our bass player</h2>
            <div class="albums">
                {% for a in dancingonglass %}
                <article class="album">
                    <a href="/dancingonglass/{{a.name}}">
                        <img src="/static/img/covers/{{a.name}}.png" alt="Album cover for {{a.title}}">
                        {{ a.title }}
                    </a>
                </article>
                {% endfor %}
            </div>
        </section>
    </main>
    
</body>
</html>